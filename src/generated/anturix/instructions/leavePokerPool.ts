/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findClosePokerPoolEscrowPda } from "../pdas";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const LEAVE_POKER_POOL_DISCRIMINATOR = new Uint8Array([
  42, 105, 195, 5, 113, 15, 109, 171,
]);

export function getLeavePokerPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LEAVE_POKER_POOL_DISCRIMINATOR,
  );
}

export type LeavePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountPokerPool extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountPokerPool extends string
        ? WritableAccount<TAccountPokerPool>
        : TAccountPokerPool,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type LeavePokerPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type LeavePokerPoolInstructionDataArgs = {};

export function getLeavePokerPoolInstructionDataEncoder(): FixedSizeEncoder<LeavePokerPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LEAVE_POKER_POOL_DISCRIMINATOR }),
  );
}

export function getLeavePokerPoolInstructionDataDecoder(): FixedSizeDecoder<LeavePokerPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLeavePokerPoolInstructionDataCodec(): FixedSizeCodec<
  LeavePokerPoolInstructionDataArgs,
  LeavePokerPoolInstructionData
> {
  return combineCodec(
    getLeavePokerPoolInstructionDataEncoder(),
    getLeavePokerPoolInstructionDataDecoder(),
  );
}

export type LeavePokerPoolAsyncInput<
  TAccountPlayer extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  pokerPool: Address<TAccountPokerPool>;
  escrow?: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getLeavePokerPoolInstructionAsync<
  TAccountPlayer extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: LeavePokerPoolAsyncInput<
    TAccountPlayer,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LeavePokerPoolInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findClosePokerPoolEscrowPda({
      pokerPool: expectAddress(accounts.pokerPool.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getLeavePokerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as LeavePokerPoolInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type LeavePokerPoolInput<
  TAccountPlayer extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  pokerPool: Address<TAccountPokerPool>;
  escrow: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getLeavePokerPoolInstruction<
  TAccountPlayer extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: LeavePokerPoolInput<
    TAccountPlayer,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): LeavePokerPoolInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountPokerPool,
  TAccountEscrow,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getLeavePokerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as LeavePokerPoolInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ParsedLeavePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    pokerPool: TAccountMetas[1];
    escrow: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: LeavePokerPoolInstructionData;
};

export function parseLeavePokerPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLeavePokerPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      pokerPool: getNextAccount(),
      escrow: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getLeavePokerPoolInstructionDataDecoder().decode(instruction.data),
  };
}
