/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findClosePokerPoolEscrowPda, findCreatorProfilePda } from "../pdas";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_POKER_POOL_DISCRIMINATOR = new Uint8Array([
  6, 232, 180, 26, 177, 249, 39, 41,
]);

export function getCreatePokerPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_POKER_POOL_DISCRIMINATOR,
  );
}

export type CreatePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountCreatorProfile extends string | AccountMeta<string> = string,
  TAccountPokerPool extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountCreatorProfile extends string
        ? WritableAccount<TAccountCreatorProfile>
        : TAccountCreatorProfile,
      TAccountPokerPool extends string
        ? WritableAccount<TAccountPokerPool>
        : TAccountPokerPool,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePokerPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  buyIn: bigint;
};

export type CreatePokerPoolInstructionDataArgs = { buyIn: number | bigint };

export function getCreatePokerPoolInstructionDataEncoder(): FixedSizeEncoder<CreatePokerPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["buyIn", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_POKER_POOL_DISCRIMINATOR }),
  );
}

export function getCreatePokerPoolInstructionDataDecoder(): FixedSizeDecoder<CreatePokerPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["buyIn", getU64Decoder()],
  ]);
}

export function getCreatePokerPoolInstructionDataCodec(): FixedSizeCodec<
  CreatePokerPoolInstructionDataArgs,
  CreatePokerPoolInstructionData
> {
  return combineCodec(
    getCreatePokerPoolInstructionDataEncoder(),
    getCreatePokerPoolInstructionDataDecoder(),
  );
}

export type CreatePokerPoolAsyncInput<
  TAccountCreator extends string = string,
  TAccountCreatorProfile extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  creatorProfile?: Address<TAccountCreatorProfile>;
  pokerPool: Address<TAccountPokerPool>;
  escrow?: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
  buyIn: CreatePokerPoolInstructionDataArgs["buyIn"];
};

export async function getCreatePokerPoolInstructionAsync<
  TAccountCreator extends string,
  TAccountCreatorProfile extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: CreatePokerPoolAsyncInput<
    TAccountCreator,
    TAccountCreatorProfile,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreatePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountCreatorProfile,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    creatorProfile: { value: input.creatorProfile ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.creatorProfile.value) {
    accounts.creatorProfile.value = await findCreatorProfilePda({
      creator: expectAddress(accounts.creator.value),
    });
  }
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findClosePokerPoolEscrowPda({
      pokerPool: expectAddress(accounts.pokerPool.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.creatorProfile),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePokerPoolInstructionDataEncoder().encode(
      args as CreatePokerPoolInstructionDataArgs,
    ),
    programAddress,
  } as CreatePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountCreatorProfile,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type CreatePokerPoolInput<
  TAccountCreator extends string = string,
  TAccountCreatorProfile extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  creatorProfile: Address<TAccountCreatorProfile>;
  pokerPool: Address<TAccountPokerPool>;
  escrow: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
  buyIn: CreatePokerPoolInstructionDataArgs["buyIn"];
};

export function getCreatePokerPoolInstruction<
  TAccountCreator extends string,
  TAccountCreatorProfile extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: CreatePokerPoolInput<
    TAccountCreator,
    TAccountCreatorProfile,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreatePokerPoolInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountCreatorProfile,
  TAccountPokerPool,
  TAccountEscrow,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    creatorProfile: { value: input.creatorProfile ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.creatorProfile),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePokerPoolInstructionDataEncoder().encode(
      args as CreatePokerPoolInstructionDataArgs,
    ),
    programAddress,
  } as CreatePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountCreatorProfile,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    creatorProfile: TAccountMetas[1];
    pokerPool: TAccountMetas[2];
    escrow: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreatePokerPoolInstructionData;
};

export function parseCreatePokerPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatePokerPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      creatorProfile: getNextAccount(),
      pokerPool: getNextAccount(),
      escrow: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePokerPoolInstructionDataDecoder().decode(instruction.data),
  };
}
