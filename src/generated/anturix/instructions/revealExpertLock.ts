/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REVEAL_EXPERT_LOCK_DISCRIMINATOR = new Uint8Array([
  221, 106, 26, 184, 74, 166, 232, 197,
]);

export function getRevealExpertLockDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVEAL_EXPERT_LOCK_DISCRIMINATOR,
  );
}

export type RevealExpertLockInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountExpert extends string | AccountMeta<string> = string,
  TAccountExpertLock extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountExpert extends string
        ? ReadonlySignerAccount<TAccountExpert> &
            AccountSignerMeta<TAccountExpert>
        : TAccountExpert,
      TAccountExpertLock extends string
        ? WritableAccount<TAccountExpertLock>
        : TAccountExpertLock,
      ...TRemainingAccounts,
    ]
  >;

export type RevealExpertLockInstructionData = {
  discriminator: ReadonlyUint8Array;
  prediction: string;
  salt: ReadonlyUint8Array;
};

export type RevealExpertLockInstructionDataArgs = {
  prediction: string;
  salt: ReadonlyUint8Array;
};

export function getRevealExpertLockInstructionDataEncoder(): Encoder<RevealExpertLockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["prediction", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["salt", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: REVEAL_EXPERT_LOCK_DISCRIMINATOR }),
  );
}

export function getRevealExpertLockInstructionDataDecoder(): Decoder<RevealExpertLockInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["prediction", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["salt", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

export function getRevealExpertLockInstructionDataCodec(): Codec<
  RevealExpertLockInstructionDataArgs,
  RevealExpertLockInstructionData
> {
  return combineCodec(
    getRevealExpertLockInstructionDataEncoder(),
    getRevealExpertLockInstructionDataDecoder(),
  );
}

export type RevealExpertLockInput<
  TAccountExpert extends string = string,
  TAccountExpertLock extends string = string,
> = {
  expert: TransactionSigner<TAccountExpert>;
  expertLock: Address<TAccountExpertLock>;
  prediction: RevealExpertLockInstructionDataArgs["prediction"];
  salt: RevealExpertLockInstructionDataArgs["salt"];
};

export function getRevealExpertLockInstruction<
  TAccountExpert extends string,
  TAccountExpertLock extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: RevealExpertLockInput<TAccountExpert, TAccountExpertLock>,
  config?: { programAddress?: TProgramAddress },
): RevealExpertLockInstruction<
  TProgramAddress,
  TAccountExpert,
  TAccountExpertLock
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    expert: { value: input.expert ?? null, isWritable: false },
    expertLock: { value: input.expertLock ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.expert),
      getAccountMeta(accounts.expertLock),
    ],
    data: getRevealExpertLockInstructionDataEncoder().encode(
      args as RevealExpertLockInstructionDataArgs,
    ),
    programAddress,
  } as RevealExpertLockInstruction<
    TProgramAddress,
    TAccountExpert,
    TAccountExpertLock
  >);
}

export type ParsedRevealExpertLockInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    expert: TAccountMetas[0];
    expertLock: TAccountMetas[1];
  };
  data: RevealExpertLockInstructionData;
};

export function parseRevealExpertLockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealExpertLockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { expert: getNextAccount(), expertLock: getNextAccount() },
    data: getRevealExpertLockInstructionDataDecoder().decode(instruction.data),
  };
}
