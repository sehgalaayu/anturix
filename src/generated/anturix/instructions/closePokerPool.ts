/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findClosePokerPoolEscrowPda } from "../pdas";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_POKER_POOL_DISCRIMINATOR = new Uint8Array([
  113, 90, 114, 89, 205, 55, 166, 61,
]);

export function getClosePokerPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_POKER_POOL_DISCRIMINATOR,
  );
}

export type ClosePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountPokerPool extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountPokerPool extends string
        ? WritableAccount<TAccountPokerPool>
        : TAccountPokerPool,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClosePokerPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClosePokerPoolInstructionDataArgs = {};

export function getClosePokerPoolInstructionDataEncoder(): FixedSizeEncoder<ClosePokerPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_POKER_POOL_DISCRIMINATOR }),
  );
}

export function getClosePokerPoolInstructionDataDecoder(): FixedSizeDecoder<ClosePokerPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClosePokerPoolInstructionDataCodec(): FixedSizeCodec<
  ClosePokerPoolInstructionDataArgs,
  ClosePokerPoolInstructionData
> {
  return combineCodec(
    getClosePokerPoolInstructionDataEncoder(),
    getClosePokerPoolInstructionDataDecoder(),
  );
}

export type ClosePokerPoolAsyncInput<
  TAccountCreator extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  pokerPool: Address<TAccountPokerPool>;
  escrow?: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getClosePokerPoolInstructionAsync<
  TAccountCreator extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: ClosePokerPoolAsyncInput<
    TAccountCreator,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClosePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findClosePokerPoolEscrowPda({
      pokerPool: expectAddress(accounts.pokerPool.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClosePokerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as ClosePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ClosePokerPoolInput<
  TAccountCreator extends string = string,
  TAccountPokerPool extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  pokerPool: Address<TAccountPokerPool>;
  escrow: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClosePokerPoolInstruction<
  TAccountCreator extends string,
  TAccountPokerPool extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: ClosePokerPoolInput<
    TAccountCreator,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClosePokerPoolInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountPokerPool,
  TAccountEscrow,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    pokerPool: { value: input.pokerPool ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.pokerPool),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClosePokerPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as ClosePokerPoolInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPokerPool,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ParsedClosePokerPoolInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    pokerPool: TAccountMetas[1];
    escrow: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClosePokerPoolInstructionData;
};

export function parseClosePokerPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClosePokerPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      pokerPool: getNextAccount(),
      escrow: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClosePokerPoolInstructionDataDecoder().decode(instruction.data),
  };
}
