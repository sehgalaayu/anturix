/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findEscrowPda } from "../pdas";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const FORCE_CANCEL_DUEL_DISCRIMINATOR = new Uint8Array([
  253, 76, 2, 204, 211, 238, 18, 208,
]);

export function getForceCancelDuelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FORCE_CANCEL_DUEL_DISCRIMINATOR,
  );
}

export type ForceCancelDuelInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountOpponent extends string | AccountMeta<string> = string,
  TAccountDuelState extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountOpponent extends string
        ? WritableAccount<TAccountOpponent>
        : TAccountOpponent,
      TAccountDuelState extends string
        ? WritableAccount<TAccountDuelState>
        : TAccountDuelState,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ForceCancelDuelInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ForceCancelDuelInstructionDataArgs = {};

export function getForceCancelDuelInstructionDataEncoder(): FixedSizeEncoder<ForceCancelDuelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FORCE_CANCEL_DUEL_DISCRIMINATOR }),
  );
}

export function getForceCancelDuelInstructionDataDecoder(): FixedSizeDecoder<ForceCancelDuelInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getForceCancelDuelInstructionDataCodec(): FixedSizeCodec<
  ForceCancelDuelInstructionDataArgs,
  ForceCancelDuelInstructionData
> {
  return combineCodec(
    getForceCancelDuelInstructionDataEncoder(),
    getForceCancelDuelInstructionDataDecoder(),
  );
}

export type ForceCancelDuelAsyncInput<
  TAccountAdmin extends string = string,
  TAccountCreator extends string = string,
  TAccountOpponent extends string = string,
  TAccountDuelState extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  creator: Address<TAccountCreator>;
  /** Pending duels have opponent = Pubkey::default() which is not system-owned. */
  opponent: Address<TAccountOpponent>;
  duelState: Address<TAccountDuelState>;
  escrow?: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getForceCancelDuelInstructionAsync<
  TAccountAdmin extends string,
  TAccountCreator extends string,
  TAccountOpponent extends string,
  TAccountDuelState extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: ForceCancelDuelAsyncInput<
    TAccountAdmin,
    TAccountCreator,
    TAccountOpponent,
    TAccountDuelState,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ForceCancelDuelInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountOpponent,
    TAccountDuelState,
    TAccountEscrow,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    opponent: { value: input.opponent ?? null, isWritable: true },
    duelState: { value: input.duelState ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findEscrowPda({
      duelState: expectAddress(accounts.duelState.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.duelState),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getForceCancelDuelInstructionDataEncoder().encode({}),
    programAddress,
  } as ForceCancelDuelInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountOpponent,
    TAccountDuelState,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ForceCancelDuelInput<
  TAccountAdmin extends string = string,
  TAccountCreator extends string = string,
  TAccountOpponent extends string = string,
  TAccountDuelState extends string = string,
  TAccountEscrow extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  creator: Address<TAccountCreator>;
  /** Pending duels have opponent = Pubkey::default() which is not system-owned. */
  opponent: Address<TAccountOpponent>;
  duelState: Address<TAccountDuelState>;
  escrow: Address<TAccountEscrow>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getForceCancelDuelInstruction<
  TAccountAdmin extends string,
  TAccountCreator extends string,
  TAccountOpponent extends string,
  TAccountDuelState extends string,
  TAccountEscrow extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: ForceCancelDuelInput<
    TAccountAdmin,
    TAccountCreator,
    TAccountOpponent,
    TAccountDuelState,
    TAccountEscrow,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ForceCancelDuelInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountCreator,
  TAccountOpponent,
  TAccountDuelState,
  TAccountEscrow,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    opponent: { value: input.opponent ?? null, isWritable: true },
    duelState: { value: input.duelState ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.duelState),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getForceCancelDuelInstructionDataEncoder().encode({}),
    programAddress,
  } as ForceCancelDuelInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountOpponent,
    TAccountDuelState,
    TAccountEscrow,
    TAccountSystemProgram
  >);
}

export type ParsedForceCancelDuelInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    creator: TAccountMetas[1];
    /** Pending duels have opponent = Pubkey::default() which is not system-owned. */
    opponent: TAccountMetas[2];
    duelState: TAccountMetas[3];
    escrow: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: ForceCancelDuelInstructionData;
};

export function parseForceCancelDuelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedForceCancelDuelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      creator: getNextAccount(),
      opponent: getNextAccount(),
      duelState: getNextAccount(),
      escrow: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getForceCancelDuelInstructionDataDecoder().decode(instruction.data),
  };
}
