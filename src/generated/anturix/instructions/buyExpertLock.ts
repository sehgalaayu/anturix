/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { ANTURIX_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const BUY_EXPERT_LOCK_DISCRIMINATOR = new Uint8Array([
  36, 253, 201, 108, 187, 135, 156, 61,
]);

export function getBuyExpertLockDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUY_EXPERT_LOCK_DISCRIMINATOR,
  );
}

export type BuyExpertLockInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountExpert extends string | AccountMeta<string> = string,
  TAccountExpertLock extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountExpert extends string
        ? WritableAccount<TAccountExpert>
        : TAccountExpert,
      TAccountExpertLock extends string
        ? ReadonlyAccount<TAccountExpertLock>
        : TAccountExpertLock,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyExpertLockInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type BuyExpertLockInstructionDataArgs = {};

export function getBuyExpertLockInstructionDataEncoder(): FixedSizeEncoder<BuyExpertLockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: BUY_EXPERT_LOCK_DISCRIMINATOR }),
  );
}

export function getBuyExpertLockInstructionDataDecoder(): FixedSizeDecoder<BuyExpertLockInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getBuyExpertLockInstructionDataCodec(): FixedSizeCodec<
  BuyExpertLockInstructionDataArgs,
  BuyExpertLockInstructionData
> {
  return combineCodec(
    getBuyExpertLockInstructionDataEncoder(),
    getBuyExpertLockInstructionDataDecoder(),
  );
}

export type BuyExpertLockInput<
  TAccountBuyer extends string = string,
  TAccountExpert extends string = string,
  TAccountExpertLock extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  buyer: TransactionSigner<TAccountBuyer>;
  expert: Address<TAccountExpert>;
  expertLock: Address<TAccountExpertLock>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getBuyExpertLockInstruction<
  TAccountBuyer extends string,
  TAccountExpert extends string,
  TAccountExpertLock extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANTURIX_PROGRAM_ADDRESS,
>(
  input: BuyExpertLockInput<
    TAccountBuyer,
    TAccountExpert,
    TAccountExpertLock,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyExpertLockInstruction<
  TProgramAddress,
  TAccountBuyer,
  TAccountExpert,
  TAccountExpertLock,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANTURIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: true },
    expert: { value: input.expert ?? null, isWritable: true },
    expertLock: { value: input.expertLock ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.expert),
      getAccountMeta(accounts.expertLock),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBuyExpertLockInstructionDataEncoder().encode({}),
    programAddress,
  } as BuyExpertLockInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountExpert,
    TAccountExpertLock,
    TAccountSystemProgram
  >);
}

export type ParsedBuyExpertLockInstruction<
  TProgram extends string = typeof ANTURIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    buyer: TAccountMetas[0];
    expert: TAccountMetas[1];
    expertLock: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: BuyExpertLockInstructionData;
};

export function parseBuyExpertLockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyExpertLockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      buyer: getNextAccount(),
      expert: getNextAccount(),
      expertLock: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyExpertLockInstructionDataDecoder().decode(instruction.data),
  };
}
